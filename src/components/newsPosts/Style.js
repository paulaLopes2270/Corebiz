import styled from "styled-components"

export const Component = styled.section`
  display: flex;
  align-items: center;
  min-height: 146px;
  padding: 24px 0;
  background: #f2f2f2;
  font-family: var(--lato);
`
